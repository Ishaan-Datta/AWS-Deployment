Thank you for installing {{ .Chart.Name }}, your release is named {{ .Release.Name }}.

To learn more about the release, try:
  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.deployment.localTesting }}
  {{- if .Values.deployment.ingressEnabled }}

For accessing all services through Ingress, try:
  $ minikube tunnel
  $ curl --resolve "mylocaltestsite.local:8080:127.0.0.1" -i http://mylocaltestsite.local/user-data/submit
  $ Test endpoints: /auth, /webapp, /recommend, /user-data
  {{- else }}

For only accessing the web application through NodePort, try: 
  $ kubectl port-forward svc/frontend 8080:8080
  $ curl http://localhost:8080
  $ minikube service web --url
  {{- end }}
{{- end}}

To uninstall: helm uninstall {{ .Release.Name }}